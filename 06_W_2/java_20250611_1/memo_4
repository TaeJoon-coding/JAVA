配列 => 同じデータ型を複数
ArrayListというのもあるとはいえ、JAVAではArrayはArray、ListはListで明確に区分されている。

可変配列、行を先に宣言し、行ごとの列を可変式で記入。
int num =
int arr[][] = new int[num][];
のように。
for( : ) => foreach, for of/in

列挙型 enum
クラス = 属性 + 行為

関数(function)とは機能を持つサブプログラム
メソッドとはオブジェクト所属の関数

EX）
自販機：関数
自販機の名前：関数名
投入口（通貨、電子マネーなど）：媒介変数（인자）
結果物出口：リターン（返還）
機械の胴体：関数の内部（body:コード部）


JAVAのパッキング！
ふつうは会社のドメインの逆順で作る。

Singleton デザイン（プログラム設計）パターン
gof의 디자인 패턴
権限設定などに注意。