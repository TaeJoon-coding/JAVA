문) 아래와 같은 UML 클래스 다이어그램 설계서를 보고 사칙연산 클래스를 작성하십시오. 

작성요령)
1. 다이어그램의 상단부의 명세를 보고 패키지(com.javateam.jse)와 그 패키지 소속의 클래스(CalcClass)를 작성합니다. 패키지는 클래스 작성시 작성될 수 있습니다. 
이때 ⓒ 아이콘의 의미는 클래스를 의미하며, 클래스의 접근 제어자는 public으로 작성합니다.

2. 클래스 설계도(클래스 다이어그램)에 멤버 필드에 대한 언급이 없으면 작성하지 않습니다.

3. 아래의 메서드의 의미를 참조하여 메서드들을 작성합니다.

예) ● add(num1 : int, num2 : int ) : int

- ● : amateras UML 툴에서는 public 접근 제어자를 의미, 
        UML 클래스 다이어그램의 원래(original) 문법에서는 "+"로 표기됨
- add : 메서드(함수) 이름
- num1 : int : int 자료형의 num1 매개변수(형식인자) 
- 맨끝의 int : 리턴(반환) 자료형

4. 아래에 제시된 메서드 add, subatrct, multiply, divide, remainder 는 각각 덧셈, 뺄셈, 곱셈, 나눗셈, 나머지를 구하는 기능의 메서드입니다.
ClacClass
add(num1: int, num2: int): int
subtract(num1: int, num2: int): int
multiply(num1: int, num2: int): int
divide(num1: int, num2: int): int
remainder(num1: int, num2: int): int